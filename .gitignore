# Log files – contain sensitive runtime error details
logs/error.log
logs/*.log

# Uploaded user content – binary files, not source code
# Track .htaccess (security) but ignore all uploaded files and subdirectories
uploads/*
!uploads/.htaccess

# Environment / secrets
.env
